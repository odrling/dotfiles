#!/sbin/openrc-run
. "${XDG_CONFIG_HOME}/rc/log.sh"

WLSUNSET_OPTS="${WLSUNSET_OPTS:--S 7:00 -s 20:00 -d 60}"

name="wlsunset"
description="set color temperature of display according to time of day"

command="wlsunset"
command_args="${WLSUNSET_OPTS}"
supervisor=supervise-daemon
