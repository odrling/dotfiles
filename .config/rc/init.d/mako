#!/sbin/openrc-run
. "${XDG_CONFIG_HOME}/rc/log.sh"

depend() {
    need dbus
}

MAKO_CONFIG="${MAKO_CONFIG:-${XDG_CONFIG_HOME}/mako/config}"

name="mako"
description="notification daemon for wayland"

command="mako"
command_args="-c ${MAKO_CONFIG}"
supervisor=supervise-daemon
