#!/bin/sh

mkfifo "$XDG_RUNTIME_DIR/dwl_status"

s6-svscanctl -t "$S6_SCANDIR" && s6-svwait -D "$S6_SCANDIR/"*
s6-svscan "$S6_SCANDIR" &

while :; do
    cat /dev/stdin > "$XDG_RUNTIME_DIR/dwl_status"
done

