#!/bin/sh
if command -v dwl > /dev/null; then
  export XDG_CURRENT_DESKTOP=wlroots
  export GDK_BACKEND=wayland
  pgrep dwl || exec dbus-run-session -- dwl
elif command -v startx > /dev/null; then
  pgrep xinit || exec startx
fi
